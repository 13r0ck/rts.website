# Still under active development
# This is a module that will be inserted into https://github.com/13r0ck/regaltechsuport.com-frontend then this repository will be deleted
run `cargo make build` in one terminal
and
run `cargo make serve` in another
them
connect in a browser @ 127.0.0.1:3000
